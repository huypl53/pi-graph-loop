# Reliability Roadmap Execution Plan

> **Status:** approved for sequential execution.
>
> Source proposal: [`reliability-roadmap.md`](./reliability-roadmap.md).
>
> This file turns the reviewed roadmap into a release-gated queue. **Only one implementation issue is active at a time.** A later issue is not assigned until the previous issue has passed independent test, review, and a fresh interactive Pi/tmux UAT.

## Non-negotiable release gates for every issue

Each issue must complete all gates below before the next issue begins:

1. **Plan gate** — exact invariants, migration/compatibility impact, negative cases, and failure-injection tests are written before production edits.
2. **Implementation gate** — scope is limited to the issue's declared files/behavior; no incidental feature additions.
3. **Independent test gate** — a tester who did not implement the change runs focused regressions plus the core suite.
4. **Review gate** — reviewer checks authority, idempotency, state mutation location, stale-write safety, and absence of scope creep.
5. **Fresh UAT gate** — in a dedicated tmux session, load the packaged extension into a fresh Pi process using the configured default model/provider; exercise one realistic success path and the relevant failure/recovery path.
6. **Evidence gate** — task artifacts contain plan, implementation report, test report, review, final summary, commands, tmux target, and captured-pane/log paths.
7. **No-go rule** — a rejected review, failed UAT, or incomplete evidence routes only to that issue's `fix` node. It blocks all later issues.

Baseline compatibility check for every issue:

```bash
NODE_PATH=$(npm root -g) npx tsc --noEmit --allowImportingTsExtensions --module nodenext --moduleResolution nodenext --skipLibCheck --target es2022 --lib es2022 extensions/swarm/index.ts
```

Bare `tsc` TS5097 caused by existing `.ts` import specifiers is not treated as a change regression.

## Sequential queue

| Order | Issue | Scope / exit condition | Status |
|---:|---|---|---|
| 1 | Authority, initial-ready nudge, nudge-policy foundation | Enforce server-side `force` / cancellation authority; alert the PM about a newly-created unassigned start node; centralize only the dedupe/cooldown/cap contract needed by this work. | Complete: `reliability-hardening-phase-1` |
| 2 | Assignment attempt / lease fencing | Add a durable assignment attempt identity and reject late updates/results from a superseded/reopened/reassigned attempt. No silent compatibility break. | Complete: `assignment-attempt-lease-fencing` |
| 3 | Cancellation and supersession | Define cancellation as lease revocation, cancel/supersede in-flight assignment messages, release resources, reject late task mutations, and provide auditable recovery. | Complete: `cancellation-and-supersession-semantics` |
| 4 | File ownership / parallel conflict policy | After leases exist, prevent unsafe overlapping concurrent write scopes or add explicit orchestrator-approved lock policy. | Complete: `file-ownership-parallel-conflict-policy-v2` |
| 5 | Recovery attention and worker reminder | Add one decision-oriented, bounded worker reminder and a synthesized orchestrator attention view. It must rely on durable assignment/message state, not idle panes as semantic proof. | Complete: `recovery-attention-worker-reminder` |
| 6 | Provider/pool preflight | Classify provider/model/tmux/pool failures before spawn/assignment and give actionable fallback/recovery output. | Complete: `pool-config-ux-and-preflight` |
| 7 | Crash consistency and failure injection | Harden and test locks/state/task/mailbox recovery under interrupted writes and concurrent mutation; document repair boundaries. | Complete: `crash-consistency-and-failure-injection` |
| 8 | Multi-orchestrator policy | Either explicitly reject a second active orchestrator or introduce and test a durable leader lease/fencing protocol. This is not enabled partially. | Complete: `multi-orchestrator-policy` |
| 9 | Lifecycle-notification fencing and stale-event suppression | Prevent an old `agent_settled` / open-assignment notification from remaining actionable after its assignment was released, superseded, cancelled, reassigned, or worker stopped. Fence creation and delivery using durable task/attempt/agent state; preserve audit without semantic mutation. | Complete: `lifecycle-notification-fencing` |
| 10 | Final surface and architecture review | Re-evaluate public tool/command exposure after operational semantics settle; keep core minimal and move debug/admin exposure behind appropriate role gates or commands. | Complete: `final-surface-architecture-review` (`6cc71c2`) |
| 11 | Orchestrator wake-up escalation | Escalating re-nudge (bounded, durable-state driven) plus optional leader-gated PM-pane wake-up via existing tmux injection when a ready node sits unassigned after the first nudge. | Complete: `orchestrator-wake-up-escalation` (`4daede3`) |
| 12 | Surface tension C6 micro-fix | Add explicit orchestrator-pane reject guard in `swarm_send_keys` so a future refactor cannot silently route raw keystrokes into the orchestrator host pane. No behavior change for valid worker→worker targets. | Complete: `surface-tension-c6-mic-fix` (`c0cdb3f`) |
| 13 | Documentation staleness audit | Audit every `docs/swarm/*.md` page against current source of truth; produce per-page diff list; apply minimal additive fixes. | Complete: `docs-staleness-audit` (`495922e`). E4/E5 deferred to follow-up. |
| 14 | Spawn-without-message engine warning | Detect operator error where `swarm_spawn_agent` returns but no `swarm_send_message` follows within a turn window. Emit a structured warning trace event (no new public tool, no hard error). Cancel on follow-up call or `swarm_assign_task` (which sends internally) or `stop_agent`. | Complete: `spawn-without-message-engine-warning` (`4547001`) |
| 15 | Operations.md deferred edits + clearReason attribution | Apply deferred E4 + E5 from Issue 13 (operations.md:139 precedence sentence; operations.md:188 heading) AND add optional clearReason parameter to deliverMessageLocked so by='swarm_assign_task' is distinguishable from by='swarm_send_message'. | Complete: `operations-deferred-edits-and-clearreason` (`41e7f66`) |
| 16 | Pre-flight spawn + assign auto-clear | Detect spawn-then-assign-by-same-orchestrator pattern and pre-clear the orphan entry before timer expires (no false-positive warning). Cross-orchestrator assign does NOT pre-clear. True orphans still fire. New constant `PREFLIGHT_ASSIGN_GRACE_MS` (default `max(5_000, ORPHAN_SPAWN_WARNING_TIMEOUT_MS - 1_000)`); `RecentSpawn` stamp fields (`spawnedByPid`, `spawnedBySessionStartedAt`); new `clearOrphanWatch(..., kind)` signature with `reason: "preflight"\|"delivery"` trace field; env-var stamp added at the top of the existing `session_start` handler in `extensions/swarm/src/hooks.ts`. | Complete: `preflight-spawn-and-assign-auto-clear` (4 review cycles) |
| 17 | Model pool rotation should respect pi engine retries | Model pool currently rotates on first error, but pi engine has its own 3-retry before giving up on a request. Pool rotation should only kick in AFTER pi retries are exhausted for the current model, not after a single transient failure. Otherwise transient 5xx/429/timeout causes unnecessary model swap, context loss, and tmux churn. | Complete: `1016d7c` + `538b368`; deferrals applied in Issue 19 commit `<hash>`. |
| 18 | Swarm goal: idle-streak nudge with back-off | swarm_set_goal + swarm_mark_goal_done tools + /swarm goal set/done commands. Orchestrator pump nudges when goal set + all agents idle. Anti-loop: 3 nudges without resolve → skip 2 ticks. | Complete: `swarm-goal-idle-nudge` (`7253efb`). |
| 19 | Model pool: apply Issue 17 deferrals + manual /swarm pool rotate override | Extract ENGINE_MAX_RETRIES + ENGINE_RETRY_WINDOW_MS to constants.ts. Add `Pi engine retry coordination` subsection to operations.md. Implement `/swarm pool rotate now` (force-swap, bypass gate, bump swap-chain) + `/swarm pool rotate next` (bench current slot, no setModel). Both orchestrator-only. | Complete: `model-pool-deferrals-and-manual-override` (`60b0dea`). |
| 20 | Auto-scaffold model pool placeholder in .pi/settings.json | On orchestrator session_start, if `swarm.modelPool` (or `extensions.swarm.modelPool` — runtime precedence) absent → write `{swarm: {modelPool: [{model: null, provider: null}]}}` + one-shot notify. Idempotent. Preserves all other settings keys. New module `extensions/swarm/src/pool-scaffold.ts`; durable flag `SwarmState.poolScaffoldNotifiedAt`; trace events `pool.scaffold_created`, `pool.scaffold_skipped_no_pi_dir`, `pool.scaffold_skipped_unparseable`, `pool.scaffold_error`. | Complete: `d8e360d` (with Issue 21). |
| 21 | Quota reset interval: bench until provider reset + recover trace event | modelPool[i].quotaResetMs?: number. Effective bench = max(cooldownMs, quotaResetMs). On bench expiry + agent has active tasks → emit `pool.slot_recovered` (orchestrator decides; no auto-resume). Env override PI_SWARM_QUOTA_RESET_MS. | Complete: `d8e360d` (with Issue 20). 7-item follow-up list deferred (constants extract, session.ts forward, command.ts:822, docs). |
| 22 | Model pool per-slot roles filter | `modelPool[i].roles?: string[]`. Optional slot-level allow-list of roleKind names. New `slotMatchesRole` helper + `pickSlot` optional `roleKind` / `bypassRolesFilter` params. `parseModelPool` (`session.ts`) forwards `roles` into the parsed `ModelSlot` so the filter is observable end-to-end. `swarm_spawn_agent` threads the spawned agent's roleKind into `pickSlot`; if every slot is filtered out, log `pool.role_filter_all_filtered_fallback` trace + retry without filter (so the worker always starts). In-process auto-swap (`hooks.ts:turn_end`) reads `agent.roleKind` from `state.agents[agentId].roleKind` under lock; the `pool.swap` trace carries `roleKind` + `rolesFilterMatched`. `/swarm pool list` renders a `roles=` column when ANY slot has roles set; `/swarm pool show` adds a `roles=[…]` line per slot. Manual `/swarm pool rotate now` passes `bypassRolesFilter:true`; trace carries `rolesIgnored:true` + `agentRoleKind`. | Complete: `8c61a2a` (32/32 + 289/289 + tsc clean; review APPROVED) |
| 23 | Task-graph-state idle nudge (no-goal variant) | New `evaluateTaskGraphStallNudgeLocked` helper in `reconcile.ts`. Mirrors Issue 18's goal-nudge back-off machinery but keyed on in_progress task state (no goal required). Per-task counter + 2-tick back-off (`MAX_TASK_STALL_NUDGES`, env-overridable via `PI_SWARM_MAX_TASK_STALL_NUDGES`). Predicate: at least one in_progress task + at least one ready+unassigned node + every non-orchestrator agent idle + age ≥ `TASK_INITIAL_READY_GRACE_MS` + not already firing the existing graph-advance nudge for the same node. Resolves on first reassignment, on a worker's claim via `swarm_update_task`, or when `applyTaskStatus` returns `terminal:true` at the existing terminal-transition sites (`tools/tasks.ts` create-task auto-close path + update-task main path + update-task second-pass after auto-close). New `NOTIFY_KEY_TASK_GRAPH_STALL` trace key + `SwarmTaskStallState` type on `SwarmState`. Goal-nudge and task-stall-nudge have independent counters with different reset triggers (turn_end vs graph-mutation). | Complete: `af43ce7` (42/42 + 49/49 + 19 regression suites + tsc clean; review APPROVED) |
| 24 | Node ownership self-heal | Five sub-deliverables: (a) `swarm_update_task` allows claim of unassigned nodes via the shared `mintNodeAttempt` helper (extracted to `taskgraph.ts`; always stamps `status=assigned`, never hybrid `ready`+`assignee`); (b) `deliverMessageLocked` auto-stamps `node.assignee` on assignment-style messages (subject + `conversationId` match `task:{taskId}:{nodeId}`); in-lock + nested `withLock` forbidden (withLock is mkdir-based and non-re-entrant); (c) every `failTaskTool` reject **IN THE §24.c COVERAGE TABLE** includes `actionableHint` or `suggestedNextCall` (full 21-site coverage deferred to follow-up `task-graph-reject-hints-coverage-audit`); (d) ownership rejections emit `task.update.ownership_reject` trace with `errorCode`; (e) assignment-mismatch emits `message.deliver.assignment_mismatch` advisory trace (reassign race or config error). New inline-string error code `OWNERSHIP_REQUIRED` (scoped to one tool + one path, NOT a ERR_* engine-wide constant). New trace events: `task.node.claimed`, `task.attempt.minted`, `task.update.ownership_reject`, `message.deliver.assignment_auto_stamp`, `message.deliver.assignment_mismatch`. | Complete: `af43ce7` (42/42 + 49/49 + 19 regression suites + tsc clean; review APPROVED) |
| follow-up | task-graph-reject-hints-coverage-audit | Audit all 21 `failTaskTool` sites in `tools/tasks.ts` for hint coverage; add hints to the 15 sites not in Issue 24's §24.c coverage table; upgrade Test 8 in `assignment-ownership.test.mjs` to a full regression guard covering all 21 sites. | Deferred |
| 25 | Minimal protocol — Phase 1 (shadow telemetry + migration) | Add `PI_SWARM_MINIMAL_PROTOCOL=0` gate (module-load read); v2 lifecycle evidence schema on `MessageRecord` (`mailboxDeliveredAt`, `seenAt`, `processingAt`, `respondedAt`, `terminalAt`, `lifecycleStage`, `lifecycleSource`, `terminalReason`, `expectResponse`, `responseDeadlineMs`, `escalateIfSilent`, `migrationRunId`, `migratedAt`); `deriveLifecycleFromTrigger` pure helper in `mailbox.ts`; `wrapSwarmToolInvocation` in `tools/wrapper.ts` wrapping every tool's `execute` for `tool.invoked` telemetry; `/swarm protocol migrate [--dry-run]` idempotent migration command; `message.lifecycle_derived_shadow` trace emissions from `swarm_check_mailbox` and the reconcile deadline sweep. Phase 1 is behavior-preserving under gate=0 — NO authoritative inferred-lifecycle writes, NO worker/orchestrator profile gating, NO removal of `[PI-SWARM ACK REQUIRED]`. Phase 2/3 deferred. | **Complete: `minimal-protocol-phase1-shadow`**. Test evidence: `extensions/swarm/minimal-protocol-shadow.test.mjs` (34 assertions), `extensions/swarm/minimal-protocol-migration.test.mjs` (23 assertions); tsc clean; smoke test still registers all 33 tools. |
| follow-up | minimal-protocol-phase-2 (gate-flip + profile gating) | Flip `PI_SWARM_MINIMAL_PROTOCOL=1` for new messages; gate profile on `roleKind` (worker 5-tool set; orchestrator 12 distinct tool names / 13 capabilities); expose `expectResponse` / `responseDeadlineMs` in normal tool schemas; remove `[PI-SWARM ACK REQUIRED]` from `formatSwarmMessageContent` body; fold `swarm_task_message` / `swarm_next_nodes` / `swarm_print_graph` into their consolidations with the two-stable-release alias window. Phase 2 requires the §H 10×2 UAT matrix passing before the gate-flip ships. | **Complete (authoritative lifecycle slice): `minimal-protocol-phase2-authoritative`**, commit `5ef238c`. Test evidence: `extensions/swarm/minimal-protocol-authoritative.test.mjs` (36 assertions, scratch-cwd isolated); shadow 34 + migration 23 regression green; tsc clean. Repo default stays gate=0; gate-flip rollout + tool-registry consolidations remain deferred behind the §H 10×2 UAT matrix. |
| follow-up | minimal-protocol-phase-2 (in flight) | **Active:** task `minimal-protocol-phase2-authoritative` — authoritative lifecycle derivation (seenAt/terminalAt) inside existing withLock; reply auto-verify with superseded fencing; terminal-update response-debt release atomically; worker reconcile rate-limit (`PI_SWARM_RECONCILE_DRYRUN_WORKER_RATE_MS`) + `scope:"self"`; `TRACE_LIFECYCLE_DERIVED` consumed at 5 sites; tier allow-lists (WORKER/ORCHESTRATOR) in `tools/gating.ts`; ACK-banner removal + identity regeneration under gate=1. Default stays gate=0; gate flip is a rollout decision. | In progress (`mp2-implementer` fix iteration + `mp2-tester` assigned) |
| 26 | Task-close worker sweep | When a task reaches a terminal status (done/failed/cancelled — via `applyTaskStatus` terminal transition sites in `tools/tasks.ts`), auto-stop every worker agent whose only active assignment was on that task, keeping only: the orchestrator, agents with remaining active tasks, paused agents, and (optionally, behind an env flag `PI_SWARM_KEEP_TASK_WORKERS=1`) everything. Emit `agent.task_sweep_stopped` trace per stopped agent (with taskId + lastAssignment release evidence) and one summary `task.workers_swept` trace. Reuse-pool agents that are merely idle but hold NO active task must ALSO be swept when a task closes only if they were spawned for that task (durable link: record `spawnedForTaskId` at spawn/assign time — additive optional field on agent record). Goal: `/swarm status` after a closed task shows orchestrator (+ any busy cross-task agents) only; the current 90-agent list of stopped ghosts is operator noise. Stopped ≠ deleted: mailbox/identity/history persist via the stable id, so a same-id restart still recovers context. Must be idempotent (double close, concurrent closes under the same withLock) and must NEVER stop an agent with an in-progress node on another task. | **Complete: `task-202608281648-issue-26-task-close-work`** (commit `a913797`). Test evidence: `extensions/swarm/task-close-sweep.test.mjs` 50/50; regressions green (authoritative 37 — one assertion legitimately superseded, shadow 34, migration 23); tsc clean; live sweep fired at task close (`agent.task_sweep_stopped` + `task.workers_swept` traces). **Follow-up gap**: `spawnedForTaskId` is only stamped on the autoSpawn/spawnIsolated fresh-spawn branch in swarm_assign_task — the common orchestrator pattern "spawn manually via swarm_spawn_agent then assign" misses the link, so task-exclusive workers stopped later than close (or lingered if their activeTaskIds pointer was already released). Stamp `spawnedForTaskId` whenever a spawn-for-node occurs or record the link at assign time for any agent whose spawn context carried the task id. |
| 27 | Goal-nudge starvation by dead/non-idle agent | Bug observed live: goal (Issue 18) is set and all genuinely-idle agents sit waiting, but the `evaluateIdleGoalNudgeLocked` predicate requires EVERY non-orchestrator agent to be `runtimeStatus="idle"`. A crashed spawn (pane never created; `tmuxAlive:false`, `runtimeStatus:"starting"` forever) or any dead/stuck pane in a non-idle status permanently blocks the all-idle predicate → goal nudge never fires → swarm stalls silently despite a live goal. Fix: exclude non-contributing agents from the predicate — treat `status="stopped"`, `!tmuxAlive`, and stale-heartbeat agents (heartbeat older than `AGENT_DEAD_HEARTBEAT_MS`, e.g. 5× pump tick) as "not in the idle equation" (they can neither work nor block). Optionally surface `goal.nudge.blocked_by` trace listing excluded agents for diagnosis. Same predicate review applies to Issue 23's `evaluateTaskGraphStallNudgeLocked` (same all-idle clause). Add failure-injection tests: (a) dead-pane agent in `starting` with goal set → nudge fires; (b) all agents stopped → nudge fires (or documented no-op when no worker exists at all); (c) healthy agent busy → nudge correctly suppressed. | **Complete** (commit `91abd0a`, folded into Issue 26 session). Test evidence: idle-nudge 52/52 incl. new case E0 (100 stopped ghosts + stale runner must not suppress), task-liveness 42/42, tsc clean. Live UAT (tmux `goal-uat`, isolated cwd): goal.set → `goal.idle_nudge` emitted 979ms later → resolved on turn_end → acked; and the production swarm's own goal nudge fired live for the first time 13.7h after being set, proving the fix. |

## R2 addendum — post-ship analysis rows (synthesized from `task-202608290900-roadmap-r2`)

> Numbered 28 → 49, continuing from Issue 27. Each row carries problem → mechanism sketch → files → acceptance sketch → impact / effort. Merged from `r2-engine` (7 findings), `r2-ops` (6 rows), `r2-protocol` (5 rows), `r2-ux` (4 rows); deduped + renumbered + ranked by impact. See `.pi/swarm/tasks/task-202608290900-roadmap-r2/artifacts/synthesis.md` for the merge rationale.

| # | Title | spec | Status |
|---:|---|---|---|
| 28 | Rework edge cannot reopen a `done` test node (INVALID_TRANSITION) | **Problem.** Default feature-dev graph `{from:"fix", to:"test", when:"implemented", rework:true}` never fires when `test` was previously `done`; worker is forced to escalate to orchestrator for a manual reopen. **Mechanism.** Widen `activateReworkNodes` guard in `extensions/swarm/src/taskgraph.ts:475-510` to `failed \| skipped \| done`; add `done → ready` to `ALLOWED_NODE_TRANSITIONS` for the orchestrator force path in `extensions/swarm/src/constants.ts:103-110`; stamp `task.attempt.reopened_by_rework` trace carrying the prior attempt id. **Files.** `extensions/swarm/src/taskgraph.ts:475-510`, `extensions/swarm/src/constants.ts:103-110`, new `extensions/swarm/rework-reopen.test.mjs`. **Acceptance.** Drive default graph to `test=done (passed)` → `fix=done (implemented)` → assert `test.status === "ready"` with a fresh active attempt, no orchestrator intervention. **Impact/effort.** P1 / S — single guard widening + transition table entry + one test. | Pending |
| 29 | Force-reopen of completed node leaves stale `activeAttemptId` + ATTEMPT_NOT_ACTIVE | **Problem.** When orchestrator force-reopens a `done` node via `swarm_update_task(status="ready", force=true)`, the prior attempt stays `status="completed"` AND `node.activeAttemptId` continues to point at it. The next `swarm_update_task` from the same assignee (no new assign yet) hits `ATTEMPT_NOT_ACTIVE` (`extensions/swarm/src/tools/tasks.ts:683-687`). `activateReworkNodes` does NOT run on this force path. **Mechanism.** In `swarm_update_task`, when transitioning terminal → non-terminal under `force=true`, clear `node.activeAttemptId`, mark the prior attempt `status="superseded", supersededBy:"<force-reopen>"`, and call `supersedeTaskAssignmentMessages` (currently cancel-only at `extensions/swarm/src/tools/tasks.ts:758`) so the prior assignment message is fenced. Require a fresh assign cycle to mint a new attempt. **Files.** `extensions/swarm/src/tools/tasks.ts:625-714`, `extensions/swarm/src/taskgraph.ts:817-845` (`mintNodeAttempt`). **Acceptance.** Complete a node → force-reopen → assert `node.activeAttemptId` is unset OR points at an active attempt the current assignee can use; update with prior attemptId fails; new assign produces a fresh attempt. Co-fix with Issue 28. **Impact/effort.** P1 (today) → P2 (after Issue 28) / S — single helper at the terminal-to-non-terminal transition + a test. | Pending |
| 30 | Both-wildcard `scopesOverlap` under-reports overlap (false negative on preflight) | **Problem.** `extensions/swarm/src/taskgraph.ts:106-110` `segmentsOverlap` both-wildcard branch tests pattern strings against each other instead of intersecting concrete filenames. `scopesOverlap("a*.ts", "*.ts")` returns NO overlap; they DO overlap. Undermines Roadmap Issue 4's file-scope ownership guarantee for any pattern containing a wildcard that isn't pure-prefix or pure-suffix. **Mechanism.** Implement a conservative "unknown → overlap=true" fallback for the restricted grammar (single-segment, only `*` wildcards, no `?`/`[]` metachars). Concretely: if BOTH patterns contain a wildcard, return `overlap:true` UNLESS one pattern is a strict subset of the other (detected by reducing the more permissive to literal form and seeing if the literal regex matches the other pattern's literal parts). Preserves preflight safety (no false-negatives); false-positive cost is bounded (orchestrator narrows scopes when ambiguous). **Files.** `extensions/swarm/src/taskgraph.ts:106-110`, new `extensions/swarm/scope-overlap.test.mjs` (~15 both-wildcard cases including the `a*.ts` / `*.ts` reproduction). **Acceptance.** `scopesOverlap("a*.ts", "*.ts") === overlap:true`; `("*.ts", "*.js") === overlap:false`; `("a*.ts", "b*.ts") === overlap:false`; `("a*.ts", "a*.js") === overlap:false`. **Impact/effort.** P1 / S — conservative fallback + test fixtures. | Pending |
| 31 | `autoCloseOrchestratorTerminalNodes` closes `commit` before orchestrator runs git | **Problem.** `extensions/swarm/src/taskgraph.ts:1116-1132` auto-stamps `commit` node `done` when its dependency `review` closes, so the orchestrator never gets a `swarm_assign_task` opportunity to claim `commit` and run the actual `git commit` + final summary. Every feature-dev task ends without committing. **Mechanism.** Remove the orchestrator-role blanket auto-close. Only auto-close orchestrator-role nodes that have NO downstream work AND are explicitly `terminal: true` AND have NO `writeArtifacts` (the engine can prove no work was owed). Co-gate: `node.role !== "orchestrator"` OR `writeArtifacts.length === 0`. Document the rule: orchestrator terminal nodes with deliverable artifacts are real work, not bookkeeping. **Files.** `extensions/swarm/src/taskgraph.ts:1116-1132`, `extensions/swarm/src/tools/tasks.ts:85-95` (create-time), `extensions/swarm/src/tools/tasks.ts:847` (update-time). **Acceptance.** Drive default graph to `review=approved` → assert `commit.status === "pending"` and the next pump tick adds it to `task.currentNodes` with a delivery to the orchestrator pane. **Impact/effort.** P1 / S — one guard + one test. | Pending |
| 32 | `spawnedForTaskId` only stamped on autoSpawn branch (root cause of orphan accumulation) | **Problem.** `extensions/swarm/src/tools/tasks.ts:317` stamps `assignee.spawnedForTaskId = task.taskId` only when `spawned === true` (the autoSpawn/spawnIsolated branch). The orchestrator's common workflow `swarm_spawn_agent(id=...)` then `swarm_assign_task(taskId, "test", agentId=...)` never writes the link, so `sweepTaskWorkersLocked` (`extensions/swarm/src/taskgraph.ts:994-1020`) treats the agent as a reuse-pool worker and only sweeps it on sole-active-task closure. Cross-task reuse lingers the agent forever. **Mechanism.** Adopt option (A): add an optional `taskId?: string` parameter to `swarm_spawn_agent` (`extensions/swarm/src/tools/agents.ts:152-185`) and stamp `spawnedForTaskId` inside `spawnAgent` so the link is owned by the spawn site (single source of truth). The orchestrator's manual-spawn-then-assign workflow passes `taskId=<taskId>` to the spawn call. **Files.** `extensions/swarm/src/tools/agents.ts:152-185`, `extensions/swarm/src/types.ts:232` (`spawnedForTaskId?: string`). **Acceptance.** Scratch-isolated task → spawn agent explicitly with `taskId` → assign → close → assert agent is stopped. Without `taskId` (legacy call shape), behavior is unchanged. **Impact/effort.** P2 / S — one new optional param + one stamp call. Root-cause companion to Issue 37. | Pending |
| 33 | Task directory archival with searchable index | **Problem.** `.pi/swarm/tasks/` accumulates one dir per task forever (67 today: 56 done, 10 cancelled/failed; largest 6.4 MB — PNG screenshots). No retention mechanism exists; operator must `rm -rf` by hand. **Mechanism.** New `swarm_archive_task({ taskId?, olderThanMs?, status?, dryRun=true, keepArtifacts?: "all"\|"markdown-only"\|"json-only" })` orchestrator tool with lock. Reads `task.json` to confirm terminal status; appends a summary line to `.pi/swarm/tasks/.archive-index.jsonl` `{ taskId, archivedAt, prevStatus, archivedTo, sha256Of(taskJson), artifactCount, artifactBytes, keptArtifacts }`; moves the dir to `.pi/swarm/tasks-archive/<YYYY-MM>/<taskId>-<shortHash>/`; drops non-matching artifacts per `keepArtifacts`; emits `task.archived` trace. New `/swarm task archive <taskId\|pattern>` command. New `/swarm task list --archived` reads the index. `swarm_task_status({includeArchived:true})` resolves archived ids without filesystem walk. Env `PI_SWARM_TASK_ARCHIVE_KEEP=markdown-only` default. **Files.** New `extensions/swarm/src/archive.ts` + `extensions/swarm/src/tools/archive.ts`; `extensions/swarm/src/state.ts` extend `Paths` with `tasksArchiveDir`; `extensions/swarm/src/constants.ts` defaults; new `extensions/swarm/archive.test.mjs` (~40 assertions). **Acceptance.** Dry-run report lists N candidates + total bytes saved; live archive moves the dir, drops non-matching artifacts, appends index, emits trace; idempotent re-archive returns "already archived"; UAT in fresh tmux target confirms `ls .pi/swarm/tasks/` no longer contains the id but `swarm_task_status` still resolves it. **Impact/effort.** HIGH (kills the 15 MB `tasks/` blob + the 6 MB screenshot outlier) / M (~1 day design + 1 day impl + 0.5 day test + 0.5 day review). | Pending |
| 34 | `swarm_gc` scheduled invocation + size-driven policy | **Problem.** `swarm_gc` exists (`extensions/swarm/src/gc.ts` + `tools/gc.ts`) but no orchestrator session invokes it. `delivered[orchestrator]` is 47 over the 500 cap; `consumerReceipts` (26 KB) and `orchestratorPumpSessions` (17 KB) never age out. **Mechanism.** Extend `pruneState` with three bounded operations: `pruneIdempotencyIndex(st, keepMessages)` (drop entries whose mapped message no longer exists, then bound), `pruneConsumerReceipts(st, keepMs=7d)`, `pruneOrchestratorPumpSessions(st, keepMs=7d)`. Extend `swarm_gc` schema with `pruneIdempotencyIndex`, `pruneConsumerReceipts`, `pruneOrchestratorPumpSessions` defaults. Add a self-rescheduling `setTimeout` watchdog in `extensions/swarm/src/hooks.ts` named `ORCHESTRATOR_GC_INTERVAL_MS` (default 30 min; env `PI_SWARM_GC_SCHEDULED=1`); first tick skipped. New `/swarm gc run [--keep-messages=N] [--dry-run]` + `/swarm gc status` commands. **Files.** `extensions/swarm/src/gc.ts` (extend `pruneState`), `extensions/swarm/src/tools/gc.ts` (extend schema), `extensions/swarm/src/hooks.ts` (watchdog using shared `runEvery` helper from Issue 36), `extensions/swarm/src/constants.ts` defaults, new `extensions/swarm/gc-scheduled.test.mjs` (~25 assertions). **Acceptance.** `swarm_gc({dryRun:false,keepMessages:500})` today drops 0 messages (none terminal-and-aged), trims idempotencyIndex, drops 0 receipts; after 7d of operation with scheduler on, file size stays within ±20% of start-of-week; `/swarm gc status` returns most-recent `gc.prune` summary. **Impact/effort.** HIGH / S-M (~0.5 day impl + 0.5 day test + 0.25 day review). | Pending |
| 35 | Mailbox JSONL size cap with trace-bundled archival | **Problem.** `.pi/swarm/mailboxes/*.jsonl` grows unbounded. `orchestrator.jsonl` is 1 MB today. `/swarm mailbox reset --yes` exists but is destructive; operators avoid it for active agents. **Mechanism.** New `appendMailbox` helper in `extensions/swarm/src/mailbox.ts` that, before appending, checks file size: if `> MAILBOX_SOFT_CAP_BYTES` (default 1 MB; env `PI_SWARM_MAILBOX_SOFT_CAP_BYTES`), atomically rotate (move `id.jsonl` → `id.<ts>.jsonl.rotated`, optionally gzip if `PI_SWARM_MAILBOX_COMPRESS=1`); prune `delivered[agentId]` ids that no longer exist in `st.messages` OR are `dead_letter`; emit `mailbox.rotated` trace. Existing mailbox reset becomes a special case that also clears `.rotated` siblings older than `MAILBOX_ROTATED_KEEP` (30d). New `/swarm mailbox rotate [agentId]` + `/swarm mailbox list` commands. **Files.** `extensions/swarm/src/mailbox.ts`, `extensions/swarm/src/state.ts` extend `Paths` with `mailboxRotatedDir`, `extensions/swarm/src/command.ts` extend `/swarm mailbox` subcommand cluster, new `extensions/swarm/mailbox-rotation.test.mjs` (~30 assertions). **Acceptance.** Append 2 MB to a fresh mailbox → `mailbox.rotated` trace, live file under cap, rotated file intact; `delivered[agentId]` after rotation contains no ids from the rotated file (intersection-safe); gzip rotation reduces bytes ≥ 60% on realistic payload. **Impact/effort.** MED-HIGH / M (~1 day impl + 0.5 day test + 0.5 day review). | Pending |
| 36 | Trace event volume reduction + tmux-capture rotation | **Problem.** `.pi/swarm/traces/events.jsonl` is 85.6 MB. Three events account for 94.2%: `mailbox.orchestrator_pump` (75.8%), `orchestrator.pump.denied` (9.9%), `notification.batch.suppressed` (8.5%). `.pi/swarm/traces/tmux/` has 1 487 files, 16.9 MB, no rotation. **Mechanism.** Drop `mailbox.orchestrator_pump` to debug-only (gated by `PI_SWARM_TRACE_PUMP=1`, default 0). Sample `orchestrator.pump.denied` to once-per-N per (agentId, reason) (default N=10; `PI_SWARM_TRACE_PUMP_DENIED_SAMPLE_EVERY`). For `notification.batch.suppressed`, emit only when `reason !== "per-tick baseline"`. Add a self-rescheduling trace rotator in `extensions/swarm/src/hooks.ts`: when `events.jsonl` exceeds `TRACE_EVENTS_SOFT_CAP_BYTES` (default 10 MB), rotate `events.jsonl` → `events.<ts>.jsonl.rotated` (optional gzip), truncate, emit `trace.rotated`. Extend `swarm_trace` schema with `includeRotated:boolean` that searches rotated files newest-first. Add tmux capture rotation: when `traces/tmux/<agent>-*.txt` count exceeds `TMUX_CAPTURE_KEEP` (default 50 per agent), prune oldest beyond cap; emit `tmux.capture.pruned`. New `/swarm trace rotate [--dry-run]` command. **Files.** `extensions/swarm/src/hooks.ts` (gate + rotator + tmux rotation using shared `runEvery`), new `extensions/swarm/src/trace.ts` (pure helpers), `extensions/swarm/src/tools/agents.ts` (extend `swarm_trace`), `extensions/swarm/src/tmux.ts` (`pruneCaptures` hook), new `extensions/swarm/trace-rotation.test.mjs` (~25 assertions). **Acceptance.** With `PI_SWARM_TRACE_PUMP=0`, 100 pump ticks produce 0 `mailbox.orchestrator_pump` events; `orchestrator.pump.denied` drops to ≤ 10% of current after one hour; file size stays under 10 MB indefinitely; `swarm_trace({includeRotated:true,limit:500})` returns lines across current + rotated files newest-first; `tmux/<agent>-*.txt` count never exceeds `TMUX_CAPTURE_KEEP=50`. **Impact/effort.** HIGH / M (~1 day impl + 0.5 day test + 0.5 day review). | Pending |
| 37 | Periodic `swarm_prune` for orphan agent records | **Problem.** 109 agent records today; all 108 non-orchestrator are `status="running"` even though 90+ have no live tmux pane, no recent heartbeat, no active task. `swarm_prune` exists but is dryRun-first, orchestrator-only, operator-initiated. Issue 26 covers task-spawned workers but leaves `swarm_spawn_agent` orphans in state forever. **Mechanism.** Self-rescheduling `setTimeout` in `extensions/swarm/src/hooks.ts` (`ORCHESTRATOR_PRUNE_INTERVAL_MS`, default 1h; env `PI_SWARM_PRUNE_INTERVAL_MS`): under `withLock`, call `swarm_prune` internally with `dryRun:false, markDead:true, removeStopped:true, stoppedOlderThanMs:24h`. Emit `prune.scheduled_run` trace. Tighten `swarm_prune` semantics for the scheduled path: only remove agents whose `lastHeartbeatAt` is older than `AGENT_DEAD_HEARTBEAT_MS` AND `tmuxAlive===false` AND `activeTaskIds.length===0` AND `status==="stopped"`. Never remove `orchestrator`. Never remove an agent with an `activeTaskIds` pointer (force `swarm_release_agent_task` first). Emit `prune.scheduled_disabled` trace when `PI_SWARM_PRUNE_SCHEDULED=0`. **Files.** `extensions/swarm/src/hooks.ts` (scheduler using shared `runEvery`), `extensions/swarm/src/tools/agents.ts` (extend `swarm_prune` or split `pruneAgentsScheduled` helper), `extensions/swarm/src/constants.ts` defaults, new `extensions/swarm/scheduled-prune.test.mjs` (~25 assertions). **Acceptance.** After one scheduled run on current state: 90+ orphan agents marked `stopped` or removed; `swarm_list_agents` returns ≈ 18 active; re-running scheduler is no-op; an agent with `activeTaskIds.length>0` is NEVER removed. **Impact/effort.** MED-HIGH / S (~0.5 day impl + 0.5 day test). Companion to Issue 32 (root cause + sweep). | Pending |
| 38 | `.pi/swarm/` gitignore + manual sweep helper | **Problem.** `.pi/` is not in `.gitignore`. `traces/tmux/*.txt` (1 487 files, 16.9 MB) shows as untracked on every fresh `git status`. Operators who `git add .` by reflex commit megabytes of runtime noise. **Mechanism.** Add a scoped `.gitignore` entry preserving operator choice: `.pi/swarm/traces/tmux/` + `.pi/swarm/backups/` + `.pi/swarm/mailboxes/` + `.pi/swarm/swarm-state.json` + `.pi/swarm/swarm-state.lock` + `.pi/swarm/agents/<id>.json` (but NOT `.pi/swarm/agents/<id>.override.md` so operators can commit overrides). Document in `docs/swarm/operations.md` §"Source control hygiene". New `/swarm sweep noise` orchestrator command (destructive, requires `--yes`): deletes `traces/tmux/<agent>-manual-<ts>.txt` files older than 7d (env `PI_SWARM_SWEEP_MANUAL_TMUX_KEEP_MS`); rotates `events.jsonl` lines older than 30d to `.rotated` (Issue 36's rotator handles live path; this is the manual one-shot); removes `backups/` files beyond `KEEP_BACKUPS * 2` (current 5; sweep trims beyond 10). Emits `sweep.noise` trace `{ deletedTmuxFiles, rotatedEventLines, deletedBackups, bytesFreed }`. **Files.** `.gitignore` (project root), `extensions/swarm/src/command.ts` (`/swarm sweep` cluster), `docs/swarm/operations.md`. **Acceptance.** After `.gitignore` change, `git status` shows zero `.pi/swarm/` files; `/swarm sweep noise --dry-run` lists N tmux manual captures + M events lines + K backups files + total bytes; `/swarm sweep noise --yes` performs cleanup + trace. **Impact/effort.** MED / XS-S (~0.25 day impl + 0.25 day test). | Pending |
| 39 | Phase 2 gate-flip rollout: §H 10×2 UAT matrix execution | **Problem.** Phase 2 authoritative lifecycle slice (commit `5ef238c`) is code-complete and unit-tested (`minimal-protocol-authoritative.test.mjs` 36 assertions), but `PI_SWARM_MINIMAL_PROTOCOL` stays at repo default 0. Per proposal §H + §I.5, gate-flip requires the 10×2 UAT matrix to pass in two isolated tmux lanes (`glm-5.1`/`zai-coding-cn` + `gpt-5.4-mini`/`openai`). **Mechanism.** (1) Add a deadline-attention trace assertion to `minimal-protocol-authoritative.test.mjs`: gate=1 must emit `message.attention.derived { source:"responseDeadlineMs" }` and `message.response.deadline_exceeded`; neither may increment `attempts` or dead-letter. Closes §K.2 / §H.4 test gap before UAT. (2) Provision two isolated tmux targets (`swarm-uat-mp2-glm`, `swarm-uat-mp2-gpt`) per AGENTS.md; load extension via `pi -e extensions/swarm/index.ts`; set `PI_SWARM_MINIMAL_PROTOCOL=1` at module load. (3) Execute the 10 scenarios (legacy ACK flow, no-ACK worker, pane-inject-dies-before-read, deadline-exceeded, superseded-stale-reply, task handoff, worker tool discovery, orchestrator goal, legacy envelope migration ×2, status graph render) in both lanes; capture pane + `events.jsonl` + state diff per scenario under `.pi/swarm/traces/uat-mp2/<lane>/<scenario>/`. (4) Per-lane pass/fail matrix; BOTH lanes PASS all ten for gate-flip approval. (5) Identity regeneration across all 5 sites under gate=1 must complete before UAT runs. **Files.** `extensions/swarm/minimal-protocol-authoritative.test.mjs` (extend), `.pi/swarm/traces/uat-mp2/<lane>/<scenario>/` (captures), `docs/swarm/minimal-agent-protocol-proposal.md` (link to matrix report), `docs/swarm/operations.md` (flip default 0 → 1 once both lanes green), optional `docs/swarm/minimal-protocol-uat-report.md`. **Acceptance.** Each lane report includes tmux target, exact pi command, scenario actions, expected vs observed, snapshot/log paths, failure notes; `tool.invoked` count for `swarm_ack_message` under gate=1 across both lanes in scenario 2 = 0; `pi.getActiveTools()` from a worker pane in scenario 7 equals exactly `{ swarm_check_mailbox, swarm_send_message, swarm_update_task, swarm_task_status, swarm_reconcile }`; both lanes PASS all ten scenarios; lane-by-lane matrix table published. **Impact/effort.** HIGH (unblocks gate-flip + rest of Phase 3) / MEDIUM. | Pending |
| 40 | Phase 3 tool consolidation: fold `swarm_task_message` / `swarm_next_nodes` / `swarm_print_graph` | **Problem.** With gate=1 enabled, three wrapper/projection tools remain in the model-facing surface (proposal §4.1 + §4.2). They add prompt overhead and confuse model-tool selection. **Mechanism.** Depends on Issue 39 + two-stable-release window. (1) Add unified schema fields `taskId`, `fromNode`, `toNode`, `artifactRefs`, `taskAssignment` (`{taskId,nodeId,attemptId}` durable token; proposal §J.2) to `swarm_send_message`. (2) Add `includeReadyNodes?:boolean`, `format?:"summary"\|"text"\|"mermaid"\|"json"`, `includeRuntime?:boolean`, `includeArtifacts?:boolean` to `swarm_task_status`. (3) Make `task.currentNodes` a derived read-time projection (not durable authoritative state); keep legacy field as write-through cache until all consumers migrate; remove writes/readers in this issue. (4) Replicate the handoff audit event from `swarm_task_message` whenever `swarm_send_message({taskId})` is called. (5) Keep `swarm_task_message` / `swarm_next_nodes` / `swarm_print_graph` as aliases for the two-stable-release window; remove aliases at end of window. (6) Identity regeneration under gate=1 to drop legacy tool references from worker / orchestrator identity cards. **Files.** `extensions/swarm/src/tools/messages.ts` (add fields; alias wrapper), `extensions/swarm/src/tools/tasks.ts` (folds + aliases), `extensions/swarm/src/taskgraph.ts` (remove `task.currentNodes` writes after consumers migrate), `extensions/swarm/src/flow-dialog.ts` (switch to derived projection), `extensions/swarm/src/reconcile.ts` (confirm derived projection), `extensions/swarm/src/identity.ts` (regenerate cards under gate=1), new `extensions/swarm/minimal-protocol-consolidation.test.mjs`. **Acceptance.** `swarm_send_message({to, body, taskId, fromNode, toNode, artifactRefs, taskAssignment:{taskId,nodeId,attemptId}})` produces same handoff audit event as legacy `swarm_task_message` for same inputs; `swarm_task_status({format,includeReadyNodes:true})` output matches legacy `swarm_print_graph` + `swarm_next_nodes` byte-for-byte; `taskAssignment` token re-verified at reply + terminal-update time; aliases continue for window with documented removal date; full §H matrix re-runs green (scenario 6 + 10 cover consolidation). **Impact/effort.** MED / MEDIUM-HIGH (touches 5+ files; careful alias compat + identity regeneration + derived-state migration). | Pending |
| 41 | Phase 3 `requiresAck` deprecation + `ack_missing` replacement | **Problem.** Under gate=1, `requiresAck` still defaults to `true` in normal schemas (many sites in `tools/messages.ts`, `mailbox.ts:261`, `reconcile.ts:98,218,363,532,857,1065`, `delivery.ts:44`); proposal §3.1 + §D end goal is `expectResponse` / `responseDeadlineMs` / `escalateIfSilent` + `ack_missing` replaced by `response_deadline_exceeded` / `no_progress`. **Mechanism.** Depends on Issues 39 + 40. (1) Add `expectResponse`, `responseDeadlineMs`, `escalateIfSilent` to `swarm_send_message` + `swarm_message` schemas. Default for new messages: `expectResponse:true`, `responseDeadlineMs: <env-default, e.g. PI_SWARM_RESPONSE_DEADLINE_MS=300_000>`. (2) `mailbox.ts:261` + every `requiresAck:true` default site switches to `expectResponse:true` for new messages; continue parsing `requiresAck` for legacy records during compat window. (3) `delivery.ts:44` adds `expectLine = showExpect && msg.expectResponse` rendering under gate=1; keep `ackLine` for legacy gate=0 records. (4) Replace `reconcile.ack_missing` trace with `message.attention.derived { source:"legacyAckMissing" \| "responseDeadlineMs" }` (proposal §K.2). (5) Add `response_deadline_exceeded` + `no_progress` to `/swarm attention` + `swarm_task_status({runtime:true})` categories; keep `ack_missing` as legacy-only bucket. (6) `flow-dialog.ts:461,677` "waiting ACK" → "waiting response" under gate=1. (7) **`/swarm remind` eligibility continues to honor legacy `requiresAck` records** (proposal §C explicit: "Continue deriving legacy `ack_missing` attention for `/swarm remind` and `deriveNodeAttention`; do not silently remove these operational inputs.") (8) At end of compat window: remove `requiresAck` from `types.ts:144,263,398`, drop consumer sites, drop `ackLine` rendering. **Files.** `extensions/swarm/src/tools/messages.ts`, `extensions/swarm/src/mailbox.ts`, `extensions/swarm/src/reconcile.ts`, `extensions/swarm/src/delivery.ts`, `extensions/swarm/src/flow-dialog.ts`, `extensions/swarm/src/taskgraph.ts:250,692`, `extensions/swarm/src/tools/agents.ts:41-42`, `extensions/swarm/src/hooks.ts:727,739,778,895`, `extensions/swarm/src/command.ts:450`, `extensions/swarm/src/constants.ts` (new env knob + `ERR_RESPONSE_DEADLINE_EXCEEDED`), `extensions/swarm/src/types.ts`, new `extensions/swarm/minimal-protocol-deprecation.test.mjs`, `docs/swarm/operations.md` §"Phase 3 governance", `docs/swarm/minimal-agent-protocol-proposal.md` §D-end governance check. **Acceptance.** New `swarm_send_message` calls with `expectResponse:false` produce NO ACK-banner under gate=1 and NO `response_deadline_exceeded` trace; legacy `requiresAck:true` records continue to be parsed and surfaced under `legacyAckMissing` attention during window; `/swarm attention` enumerates new categories; `ack_missing` count tracks only legacy records; `flow-dialog.ts` renders "waiting response" for new + "waiting ACK (legacy)" for legacy; at end of window `requiresAck` removed from `types.ts` + consumer sites compile-clean. **Impact/effort.** MED-HIGH / MEDIUM (mostly additive; hard parts are legacy-record parsing window + identity regeneration). | Pending |
| 42 | Phase 3 admin-mode tool demotion (`swarm_message_status`, `swarm_attach_agent`, full `swarm_agent_identity`) | **Problem.** Proposal §10 marks `swarm_message_status`, `swarm_attach_agent`, and full `swarm_agent_identity` reader as admin/diagnostic only. Today they remain in orchestrator allow-list (`extensions/swarm/src/constants.ts:268-296`) under gate=1, broader than the proposal allows. **Mechanism.** (1) Remove `swarm_message_status` + `swarm_attach_agent` from `ORCHESTRATOR_TOOL_ALLOWLIST`; retain registration; gate on `PI_SWARM_ADMIN_MODE=1` or slash command only. (2) Add `includeIdentity?:boolean` to `swarm_agent_status` (Issue 40 same-module dependency); implement summary fields (role, roleKind, capabilities, tmuxTarget, mailbox path, identityFile path); remove `swarm_agent_identity` from orchestrator allow-list; gate on admin mode. (3) Update `tools/gating.ts` admin branch to ensure all three tools surface only when `PI_SWARM_ADMIN_MODE === "1"` OR caller id is `"admin"`. (4) Slash commands that previously routed through these tools continue to work because they bypass the active-set filter. **Files.** `extensions/swarm/src/constants.ts` (allow-list edits), `extensions/swarm/src/tools/gating.ts` (admin branch), `extensions/swarm/src/tools/agents.ts` (`includeIdentity` param), `extensions/swarm/src/command.ts` (confirm slash commands), new `extensions/swarm/minimal-protocol-admin-gating.test.mjs`. **Acceptance.** Non-admin orchestrator's `pi.getActiveTools()` excludes `swarm_message_status`, `swarm_attach_agent`, `swarm_agent_identity`; admin orchestrator includes them; slash commands `/swarm agents attach <id>`, `/swarm message status <id>`, `/swarm agent identity <id>` continue to function. **Impact/effort.** LOW-MED / LOW (three allow-list edits + admin gating branch + identity summary in `swarm_agent_status`). Ships AFTER Issue 39 (UAT must validate the broader surface as-shipped today). | Pending |
| 43 | Gate-flip no-go review (process gate at end of compat window) | **Problem.** Per proposal §D end, the release gate at the end of the two-stable-release window must explicitly review migrated-state audit, stale legacy records, dead-letter effects, deadline traces, normal-flow ACK-call count, and recovery/UAT evidence BEFORE removing `requiresAck`. Without explicit checklist, deprecation can drift. **Mechanism.** (1) Author `docs/swarm/minimal-protocol-deprecation-checklist.md` with §D-end review items, each tied to a measurable artifact (e.g. "no `ack_missing` count > 0 across most-recent 1k `tool.invoked` traces", "zero `requiresAck:true` records in `/swarm attention` `ack_missing` category for messages created after window start"). (2) Add `/swarm protocol deprecation-readiness` slash command (orchestrator-only) that aggregates review items from durable state + `events.jsonl` + `tool.invoked` traces; emits PASS/FAIL/BLOCKED per item. (3) Issue 41 final removal of `requiresAck` is gated on `/swarm protocol deprecation-readiness` returning PASS for all items. Advisory only — does NOT auto-block gate-flip. **Files.** New `docs/swarm/minimal-protocol-deprecation-checklist.md`, `extensions/swarm/src/command.ts` (`/swarm protocol deprecation-readiness`), new `extensions/swarm/minimal-protocol-readiness.test.mjs`. **Acceptance.** `/swarm protocol deprecation-readiness` enumerates §D-end review items; each references its evidence source (file + line range); removing `requiresAck` from `types.ts` and consumer sites is blocked by a failing readiness report (advisory; orchestrator judgment required to override). **Impact/effort.** MED / LOW (documentation + one slash command + tests). | Pending |
| 44 | Status rollup live-first + dedupe late settle notifications | **Problem.** `/swarm status` was too noisy. Ghost/stopped agents dominated. `response_missing` is technically accurate but reads like a failure. Late settle notifications from stopped agents (e.g. `mp2-implementer` x4) kept arriving. **Mechanism.** Change default `/swarm status` hierarchy: (a) live running agents first; (b) response debt as friendlier label `waiting on reply/result`; (c) collapsed historical ghosts/stopped agents with count + opt-in expansion (`--all`, `--verbose`, or follow-up surface). Keep raw `runtimeStatus` for machine use; default human label explains state + next action. For stale settle notifications: collapse repeated late arrivals from same stopped agent into single advisory with suppression count instead of surfacing each independently. **Files.** `extensions/swarm/src/reconcile.ts`, `extensions/swarm/src/command.ts`, `extensions/swarm/src/hooks.ts`. **Acceptance.** `/swarm status` shows concise live-first summary; stopped agents no longer dominate default view; `response_missing` explained in operator terms; repeated settle notifications from same stopped agent deduped into one visible advisory. **Impact/effort.** HIGH / MEDIUM. | Pending |
| 45 | Goal reminder cadence legibility | **Problem.** Operator sees `nudge 1 of 3` but not when next reminder fires, so reminder feels arbitrary rather than scheduled. **Mechanism.** Add explicit cadence text to goal nudge body: `next reminder in ~2 pump ticks unless the goal resolves; 3 nudges then 2-tick back-off`. Make `/swarm goal` discoverable from top-level operator flow: `/swarm status` mentions active goal summary + hint for `/swarm goal show`; command help surfaces `goal show` alongside `goal set/done`. Consider compact status line for current goal so operators don't have to remember the subcommand. **Files.** `extensions/swarm/src/reconcile.ts`, `extensions/swarm/src/command.ts`, `docs/swarm/tools.md`. **Acceptance.** Goal nudge text tells operator when next reminder is expected; cadence/back-off is legible; `/swarm status` or `/swarm help` exposes how to inspect active goal without guessing command shape. **Impact/effort.** HIGH / SMALL. | Pending |
| 46 | Timestamp parity across operator surfaces (extends `11c2807`) | **Problem.** Timestamp prefixes were added in `11c2807` for delivered messages, but not every operator-facing surface carries them yet. `swarm_check_mailbox` / mailbox output and `/swarm status` still need consistent visible time anchor. **Mechanism.** Standardize compact time prefix or age field on all operator-facing swarm surfaces: `/swarm status` task lines, `swarm_check_mailbox` output, `/swarm task` + goal summary surfaces. Keep it short + local-time friendly so it does not overwhelm the TUI; make "how old is this?" answer visible without drilling into traces. **Files.** `extensions/swarm/src/delivery.ts`, `extensions/swarm/src/reconcile.ts`, `extensions/swarm/src/tools/messages.ts`, `extensions/swarm/src/command.ts`. **Acceptance.** Operator can look at status/mailbox output and immediately see staleness/recency without opening traces; timestamp format consistent across surfaces; no surface silently omits time anchors. **Impact/effort.** MED / SMALL. Extends `11c2807` baseline. | Pending |
| 47 | Goal discoverability in `/swarm status` | **Problem.** `/swarm goal show` exists but is hidden behind syntax knowledge instead of being a first-class inspect action. **Mechanism.** Add discoverable goal summary card in main status flow + help hint pointing to `/swarm goal show`. If a goal exists, `/swarm status` includes active goal text, its age, and its nudge state in one place. If no goal exists, the status line tells the operator that explicitly and mentions how to create one. **Files.** `extensions/swarm/src/command.ts`, `extensions/swarm/src/reconcile.ts`, `docs/swarm/tools.md`. **Acceptance.** Operator can discover goal inspection from main status surface in one step; active goals visible at a glance; "no active goal" state tells operator what to do next. **Impact/effort.** MED / SMALL. Companion to Issue 45 (different problem: nudge body vs status surfacing). | Pending |
| 48 | `closingAssignee` capture predicate gap (PM-notify smell post-rework) | **Problem.** `extensions/swarm/src/tools/tasks.ts:721` captures `closingAssignee = node.assignee || undefined` immediately after `releaseNodeAssignment` (which doesn't clear `node.assignee`). Then `checkClosureNotificationStale` (`extensions/swarm/src/taskgraph.ts:368-376`) checks "did the new assignee differ from the closing one?" — but the closing one is the OLD one (about to be cleared by `activateReworkNodes`). If the orchestrator reopens + reassigns to the SAME agent, predicate returns not-stale and notify fires even though the node was just reopened. Logic smell more than hard bug. **Mechanism.** Add a third predicate branch in `checkClosureNotificationStale`: `node.status === "ready" && !node.assignee && reopenedThisTick === true`. Plumb a `recentlyReopened` flag through from `activateReworkNodes` (or return list of reopened node ids + timestamps; check at the `task.update` call site). Rename `closingAssignee` → `assigneeAtClose` to make the capture order obvious to future readers. **Files.** `extensions/swarm/src/tools/tasks.ts:721`, `extensions/swarm/src/taskgraph.ts:368-376`. **Acceptance.** Drive a rework edge that reopens a downstream node; assert notify to orchestrator either fires a dedicated `task.reopened` subject OR is suppressed per the predicate. **Impact/effort.** P2 / S (plumb reopened flag + extend predicate). Becomes relevant only after Issue 28 ships. | Pending |
| 49 | Phase 2 reply auto-verify clobbers `verifiedAt` on second reply | **Problem.** `extensions/swarm/src/mailbox.ts:386-396` Phase 2 reply branch calls `validateResultMessage` and on success flips `original.response.status = "verified", resultMessageId: m.id, verifiedAt: now()`. Does NOT check whether `original.response.status` is ALREADY `verified`. A second reply (e.g. worker follow-up after orchestrator already verified first reply) overwrites `verifiedAt` and `resultMessageId`. Erases audit trail: which reply was the canonical completion signal? `validateResultMessage` itself has no idempotency check (`mailbox.ts:64`). **Mechanism.** At the top of the verified branch, gate on `original.response?.status !== "verified"`: if already verified, append to a separate `followUpMessageIds` array; do NOT overwrite `verifiedAt`/`resultMessageId`. Trace `message.response.followup` with `{ originalResultMessageId, followUpMessageId }`. Add `followUpMessageIds?: string[]` to `MessageRecord` type. **Files.** `extensions/swarm/src/mailbox.ts:386-396`, `extensions/swarm/src/mailbox.ts:64`, `extensions/swarm/src/types.ts` (extend `MessageRecord`). **Acceptance.** Deliver reply #1 (status flips to verified, verifiedAt = t1, resultMessageId = m1); deliver reply #2 (status stays verified, verifiedAt = t1, resultMessageId = m1, followUpMessageIds = [m2]); follow-up trace fires; first reply's evidence is not overwritten. **Impact/effort.** P2 / S (one branch + one type field + one test). | Pending |

## R3 addendum — post-Issue-28 rows (synthesized from `task-202608291005-issue-28-rework-reopen/artifacts/r3-analysis.md`)

> Post-ship 5-person analysis (r2-protocol, r2-ux, r2-ops, r3-ritual; r2-engine ghosted mid-ritual — itself evidence for row 51). Deduped against the R2 addendum: analyst rows R35/R33/R36/R37/R38/R32 mapped to existing Issues 32/39/44/45/47/49 respectively and are NOT duplicated here. New rows numbered 50 → 55.

| # | Title | spec | Status |
|---:|---|---|---|
| 50 | `activateReworkNodes` drops `process.cwd()` dependency (Paths threading) | **Problem.** `extensions/swarm/src/taskgraph.ts:502` emits `task.attempt.reopened_by_rework` via `trace(paths(process.cwd()), ...)` — the trace target depends on the process cwd at call time; the sweep path (`taskgraph.ts:1035,1054`) already threads the caller's `Paths` and is the convention to follow. Flagged as non-blocking by the Issue 28 review. **Mechanism.** Change signature to `activateReworkNodes(task, p: Paths)`, pass `p` from the `tools/tasks.ts:721` call site (already inside `withLock(p)`), drop `process.cwd()`. Single production call site. Fold in the adjacent review follow-up: simplify the dead-arm `priorStatus` expression (`priorAttempt ? "done" : (priorActiveAttemptId ? "unknown" : "done")` → `priorAttempt ? "done" : "unknown"`). **Files.** `extensions/swarm/src/taskgraph.ts`, `extensions/swarm/src/tools/tasks.ts`, extend `extensions/swarm/rework-reopen.test.mjs` (scratch-cwd trace-target assertion). **Acceptance.** Scratch-cwd test asserts the reopened_by_rework trace lands in the scratch task's events file, not the process-cwd one; rework-reopen suite stays green. **Impact/effort.** M / S — 5-line refactor + assertion. | Pending |
| 51 | Auto-retarget agent tmuxTarget by window-name on injection failure (window-renumber ghosts) | **Problem.** Task-close sweeps kill windows, tmux renumbers the rest, and agent records keep stale absolute targets (`pi-swarm-...:10.0` after the window moved to 9). Happened twice in one session (mp2-tester `:10.0`→`:9.0` — injection failed `can't find window: 10`; r2-engine ghosted mid-ritual with `status:stopped` + `tmuxAlive:true`, a combination that should be impossible). Assignment messages sit pending forever; agents silently drop out of the reuse pool. **Mechanism.** When tmux injection (or capture) fails with `can't find window/session`, look up the agent's window by NAME (`tmux list-windows -t <session>`, match `window_name == agentId`), rewrite `agent.tmuxTarget` to the current index, retry the injection once, and emit `agent.tmux_retargeted{from, to, by: "window-name-lookup"}`. If the window name no longer exists either, mark the agent stopped (real ghost). Also reconcile the `tmuxAlive` boolean against the retarget outcome so `stopped+tmuxAlive:true` cannot persist. **Files.** `extensions/swarm/src/tmux.ts` (window-name lookup helper), `extensions/swarm/src/delivery.ts` (injection failure path), `extensions/swarm/src/tools/agents.ts` (swarm_register_agent reuse), new `extensions/swarm/tmux-retarget.test.mjs`. **Acceptance.** Simulate a renumber (kill a lower-index window) → assignment injection self-heals on first retry with the trace; agent with no matching window name is marked stopped, not retargeted; `stopped+tmuxAlive:true` combo eliminated by reconciliation. **Impact/effort.** M-H / S-M — one helper + one retry path; kills a whole ghost class. | Pending |
| 52 | Goal-nudge body is a stale snapshot (refresh at surfacing time) | **Problem.** The goal-nudge body is rendered from the state snapshot at EVALUATION time, but the pump may defer surfacing for minutes while the orchestrator turn runs — today 3+4 stale nudges surfaced "no active work" text seconds AFTER new work was assigned (and 3 queued nudges surfaced together at turn end). Operators acted on outdated state; trust erodes. **Mechanism.** At surfacing time, re-read live state and rebuild the nudge body: fresh idle-streak count, fresh agent/activity summary (are there now assigned/in_progress nodes? then either re-render or suppress the nudge entirely as resolved). Emit `goal.nudge.snapshot_refreshed` when the rebuilt body differs materially from the evaluated one; suppress rather than surface a nudge whose condition no longer holds. Adjacent tuning observed by r2-ops: 60 `goal.nudge.backoff.skip`/`.exhausted` events vs 12 real fires in 4h — after this fix, back-off ticks that would surface an already-resolved nudge should be no-ops. Ships with Issue 45 cadence legibility ("next reminder in ~N ticks" line). **Files.** `extensions/swarm/src/reconcile.ts` (nudge body construction + surfacing path), `extensions/swarm/idle-nudge.test.mjs` (stale-snapshot case). **Acceptance.** Test: evaluate emits nudge → assignment lands → surfacing rebuild detects active node → nudge suppressed or re-rendered with fresh text, `snapshot_refreshed` traced; zero "no active work" bodies surfaced after work exists. **Impact/effort.** M / S-M. | Pending |
| 53 | Test coverage for stale-lease self-heal (follow-up to `a23083b`) | **Problem.** The stale-lease reclaim (`a23083b`) shipped with a live UAT but no unit test pinning the behavior; r2-ops observed 444 `pump.denied{state:stale}` events from a single pid in 4h with 0 `lease_reclaimed` events over 26 days (the live recovery post-reload was the fix working, pre-reload traces are the deadlock itself). A regression here silently freezes the orchestrator again. **Mechanism.** Unit test in `multi-orchestrator.test.mjs` (or a dedicated file): seed a state file with a lease older than `ORCHESTRATOR_LEADER_STALE_MS` held by the SAME pid, run one pump tick, assert `orchestrator.pump.lease_reclaimed` emitted and the tick completed (not denied); second case: stale lease held by a DEAD pid with no competing live claim → reclaimed too; third case: live lease held by a DIFFERENT pid → still denied (`reclaimedStale` path in `claimOrchestratorLeader` race). **Files.** `extensions/swarm/multi-orchestrator.test.mjs`. **Acceptance.** The three cases above pass; regression suite green. **Impact/effort.** M / S — pure test work. | Pending |
| 54 | Informational orchestrator messages over-flag `requiresResponse` | **Problem.** Orchestrator follow-up/instruction messages (task-message informational sends, protocol cleanup notices) default `replyExpected:true` on `swarm_task_message`, creating false `response_missing` blocks when the recipient treats them as FYI — happened live today (an instruction to clear a block itself became a required-response message, doubling the block count). **Mechanism.** Make `swarm_task_message` default `replyExpected:false` unless the caller passes it explicitly, and document in the orchestrator identity card: informational notices → no reply expected; assignments and questions → reply expected. (Post-Issue-41 world: default `expectResponse:false` for informational sends aligns with the Phase 3 governance.) **Files.** `extensions/swarm/src/tools/tasks.ts` (task_message schema default), `extensions/swarm/src/identity.ts` (orchestrator card guidance). **Acceptance.** `swarm_task_message` without explicit `replyExpected` creates a message with `requiresResponse:false`; assignment messages still default true (they carry their own semantics); identity regenerated. **Impact/effort.** M / S. | Pending |
| 55 | Reply auto-verify for `requiresResponse` records (adjacent to Issue 49) | **Problem.** Live today: a plan assignment (`requiresResponse:true`) received a real reply, yet `response.status` stayed `missing` because verification runs only on the orchestrator's explicit ack path — the worker then sat `response_missing`-blocked until manual protocol cleanup. **Mechanism.** In the reply branch (`tools/messages.ts`), when the original record is `requiresResponse:true` and a reply with `replyTo` arrives, auto-verify the response (`response.status="verified", resultMessageId=<reply id>, verifiedAt=now()`), emitting `message.reply_auto_verified`. Keep Issue 49's already-verified clobber-guard: a SECOND reply after auto-verify appends `followUpMessageIds` instead of overwriting. Legacy gate=0 `requiresAck`-only records keep the existing manual-verify path. **Files.** `extensions/swarm/src/tools/messages.ts`, `extensions/swarm/src/mailbox.ts`, `extensions/swarm/reply-auto-verify.test.mjs`. **Acceptance.** Assignment → worker replies → auto-verified without orchestrator intervention; second reply becomes followUp, no overwrite; manual ack path still works for legacy records. **Impact/effort.** M / S-M. | Pending |

## R4 addendum — post-Issue-29 rows (synthesized from `task-202608291105-issue-29-force-reopen-attempt/artifacts/r4-analysis.md`)

> Post-ship 5-person analysis after the 3-round Issue 29 cycle (r2-ops, r2-ux, i28-impl, mp2-tester, r3-ritual). New rows 56 → 67. Analyst-proposed grouping also recorded: commit-node-state-machine (57+59), attestation-and-honesty (58+61+62), settle-notify-and-trace-noise (56+60), reopen-path-semantics (63+64+65+66).

| # | Title | spec | Status |
|---:|---|---|---|
| 56 | `agent_settled` false-positive heuristic (min-gap + cooldown) | **Problem.** 94 `message.response_missing.settled.notify` events today, ~95% spurious: the settle handler checks missing-responses at SETTLE time, but a busy agent (long tool run) settles transiently and replies minutes later (mp2-tester: settle at 13:15, actual ack 13:27; median delay 315s). Operators see 6+ false "settled with missing response(s)" per session. **Mechanism.** Gate the `missingResponses`/`activeTaskIds` check in the `agent_settled` handler (hooks.ts:680-795) on: (a) minimum gap — only fire if `lastToolAt` is older than `SETTLE_NOTIFY_MIN_GAP_MS` (default 10 min, env-tunable); (b) cooldown — skip if the previous settle notify for this agent fired within `SETTLE_NOTIFY_COOLDOWN_MS` while its last ack landed within the last minute. Est. collapse 94 → ~5 events/day with identical true-positive coverage. **Files.** `extensions/swarm/src/hooks.ts`, `extensions/swarm/src/constants.ts`, `extensions/swarm/settle-notify.test.mjs`. **Acceptance.** Injected timeline: tool_running → settle → reply 5 min later produces ZERO notifies; settle after 15 min of true silence with a still-missing response produces exactly one. **Impact/effort.** H / M. | Pending |
| 57 | Commit-node state progression + block auto-close until proof (engine gap #4) | **Problem.** Second occurrence today: `commit` node auto-stamped `done(committed)` before the orchestrator ran git (cron task first, Issue 29 second). The operator cannot distinguish "claimed committed" from "actually committed". **Mechanism.** Sub-state machine for commit-class nodes: `committed-claimed` → `committed-awaiting-evidence` → `committed-confirmed`. Block the auto-close transition until `committed-confirmed` is stamped by a `git_verified` check (HEAD moved past task baseline / new commit contains the task's file set) or an explicit `swarm_confirm_commit` orchestrator tool. Keep such nodes out of terminal-looking status surfaces (render under "commit confirmation pending"). **Files.** `extensions/swarm/src/taskgraph.ts` (auto-close gate), `extensions/swarm/src/tools/tasks.ts`, `extensions/swarm/src/command.ts` (render), `extensions/swarm/commit-state.test.mjs`. **Acceptance.** Default graph driven to review=approved: commit node enters awaiting-evidence, does NOT auto-close; after a git commit whose diff touches the task's files lands, confirmation stamps done; status never shows a bare done commit without evidence. **Impact/effort.** HIGH / M. | Pending |
| 58 | Trace-backed assertion attestation for implementer test claims | **Problem.** Issue 29 round 1: implementation-report claimed "Passed: rework-reopen.test.mjs" while the suite failed 25/28 — caught only by the tester's independent re-run. Round 2 repeated the pattern ("reverted to committed form" while a contract field still deviated). **Mechanism.** Every "Passed:" claim in implementation/fix reports must cite `{tool, eventId, ts}` of the actual `tool.invoked`/bash-exit events in `.pi/swarm/traces/events.jsonl`. Add a pre-review guard (or `swarm_attest_evidence` tool) that cross-checks: cited event exists, timestamp matches, recorded exit/result is consistent with the claim. Fake passes leave an unfakeable gap. **Files.** `extensions/swarm/src/tools/tasks.ts` (update-time guard), report schema guidance in identity/plan templates, `extensions/swarm/attestation.test.mjs`. **Acceptance.** Report citing a non-existent event id or an event with a non-zero exit → update rejected with actionable error; honest report with matching evidence passes untouched. **Impact/effort.** HIGH / S. | Pending |
| 59 | Coalesced "what changed" banner for repeated state flips | **Problem.** Repeated late-settle markers + status flips on the same worker flood the operator surface (extends Issue 44). **Mechanism.** Coalesced one-line banner summarizing repeats ("worker X: still working; 1 stale settle suppressed; commit confirmation pending") with drill-in to details; compose after row 56 removes spurious settles. **Files.** `extensions/swarm/src/command.ts`, `extensions/swarm/src/hooks.ts`. **Acceptance.** Three repeated flips render as one line; drill-in shows all three with timestamps. **Impact/effort.** M / M. | Pending |
| 60 | Goal-nudge back-off bookkeeping traces collapsed to one transition trace | **Problem.** Live: 60 `goal.nudge.backoff` + 60 `.skip` + 60 `.exhausted` vs 13 real `goal.idle_nudge` — bookkeeping is ~85% of goal events. **Mechanism.** Keep back-off STATE on the goal record but stop emitting per-tick bookkeeping traces; emit ONE `goal.nudge.transition {goalId, fromState, toState, sinceLastResolveMs}` on back-off arm/lift and resolve. Sits inside Issue 36's trace-volume work. **Files.** `extensions/swarm/src/reconcile.ts`. **Acceptance.** A 3-nudge + backoff + resolve cycle emits exactly 4 goal events (3 nudges + 1 transition) instead of ~20. **Impact/effort.** M / S. | Pending |
| 61 | Test-report schema rule: raw output + git-diff-vs-baseline required | **Problem.** Reports can claim anything; the visual diff + raw tails are the operator's only ground truth (companion to row 58). **Mechanism.** Plan-template checklist + reviewer guard: every test-report includes `tail -N` of each suite run and `git diff --stat <baseline>..working`; reviewer rejects reports lacking them. **Files.** plan/report templates in identity or docs, reviewer checklist. **Acceptance.** A report without raw output snippets is rejected by the review gate. **Impact/effort.** M / S. | Pending |
| 62 | Mechanical allowed-files gate on implementer done | **Problem.** Issue 29 round 1 silently modified `attempt-fencing.test.mjs` (NOT in allowedFiles), weakening an assertion — caught only by manual git status review. **Mechanism.** In `swarm_update_task`, when accepting `outcome=implemented` from an implementer, run `git diff --name-only <baseline>` and reject if any path falls outside the task's `allowedFiles` (empty allowedFiles = unrestricted, current semantics). Auto-attach `git diff --stat` to the acceptance. **Files.** `extensions/swarm/src/tools/tasks.ts`, `extensions/swarm/allowed-files-gate.test.mjs`. **Acceptance.** Out-of-scope edit → update rejected naming the offending path; in-scope edits pass; unrestricted tasks unaffected. **Impact/effort.** M / S. | Pending |
| 63 | Force-reopen resets `node.attempts` budget (Issue 29 residual F1) | **Problem.** Live-verified: force-reopen on default-graph `plan` (maxAttempts=1) leaves the node unassignable — reassign rejected with `attempts:1 >= maxAttempts:1`; the reopened node is dead until task.json is hand-edited. **Mechanism.** In the force-reopen branch, either reset `node.attempts` to 0 (audit preserved in attemptHistory) or add a force-bypass on the maxAttempts guard when `prevStatus` was terminal && `force=true` && non-terminal target. **Files.** `extensions/swarm/src/tools/tasks.ts`, extend `extensions/swarm/force-reopen-attempt.test.mjs`. **Acceptance.** Default-graph plan force-reopened then reassigned succeeds; maxAttempts semantics unchanged for non-force paths. **Impact/effort.** M / S. | Pending |
| 64 | One-shot rework edge per activation (Issue 29 residual F2) | **Problem.** Rework edges can re-fire repeatedly: `activateReworkNodes` has no consumed/epoch semantic, so a hot fix→test cycle can reopen the same node in a loop (pre-existing since Issue 28; masked by a weakened test assertion). **Mechanism.** Track edge consumption (`edge.consumedAt` stamped on activation; reset on orchestrator force-reopen of the upstream node) or a node-level `reopenEpoch` compared against the edge's last-fired epoch. Restore the weakened `attempt-fencing.test.mjs:218` assertion (`node.status === "done"`) once shipped (row 67 rides here). **Files.** `extensions/swarm/src/taskgraph.ts`, `extensions/swarm/rework-once.test.mjs`. **Acceptance.** fix→test rework fires exactly once per completion cycle; second completion without a new fix lands does not re-fire; force-reopen resets consumption. **Impact/effort.** M / M. | Pending |
| 65 | Default-graph conformance tests for reopen/rework paths | **Problem.** Custom-node tests passed while the default feature-dev graph broke live (maxAttempts residual caught ONLY because UAT happened to use the default graph). **Mechanism.** Add default-graph assertions for `node.attempts`, one-shot rework behavior, and trace-location contracts to the EXISTING suites (no new files). **Files.** `extensions/swarm/force-reopen-attempt.test.mjs`, `extensions/swarm/rework-reopen.test.mjs`. **Acceptance.** A regression of the row-63/64 class in the default graph fails at least one suite. **Impact/effort.** M / S. | Pending |
| 66 | Restore weakened `attempt-fencing.test.mjs:218` assertion (after row 64) | **Problem.** The assertion `node.status === "done"` was weakened in Issue 29 round 1 (to `attemptHistory.at(-1).status === "completed"`) to mask residual F2. **Mechanism.** Revert to the original once row 64's one-shot semantics ship. **Files.** `extensions/swarm/attempt-fencing.test.mjs`. **Acceptance.** Original assertion green under one-shot rework semantics. **Impact/effort.** L / XS. | Pending |
| 67 | Validation-lane docs: plain `pi`, no double `-e`, zai auth note | **Problem.** Two validation lanes died today on `No API key found for zai-coding-cn`; `pi -e <path>` double-loads the bundled swarm extension (33 conflicts observed). Already partially fixed in `990db7f` (AGENTS.md/docs/skill now lead with bare `pi`); residual: document the `-e` double-load trap and the auth registration step. **Mechanism.** Amend AGENTS.md spawn-rules + operations quick-start: prefer plain `pi`; if explicit extension loading is needed use `-ne -e ...` to avoid double-load; document `pi auth` registration for zai lane. **Files.** `AGENTS.md`, `docs/swarm/operations.md`. **Acceptance.** A fresh agent following the docs boots a validation pi without key errors or extension conflicts. **Impact/effort.** M / XS. | Pending |
| 68 | Nudge semantics redesign — graph-idle action vs time-anchored goal fallback (USER-SPEC, supersedes prior row 68 wording) | **AUTHORITATIVE USER LOGIC.** Two separate paths; never conflate them. **Path A — task-graph nudge (priority):** emit when (i) a task is non-terminal, (ii) it has actionable `ready`/unassigned node(s), AND (iii) ALL effectively-live non-orchestrator agents are idle (stopped/dead/stale ghosts excluded per Issue 27). It is immediate/actionable — names task/node/role and assignment action; it is NOT governed by the goal interval. **Path B — goal nudge (fallback):** emit only when a goal exists, NO actionable-unassigned node exists in ANY unfinished graph, and all effective agents have been continuously idle for a configured elapsed-time interval measured from the BUSY→ALL-IDLE EDGE. Current code violates B: no idle-since field exists; it emits once per 5s pump tick (live seq 18/19/20 in 15s). Current code also permits A+B same-tick double notifications (live 15:44:33). **Mechanism.** Add swarm-level (NOT inside `goal`, so set/refresh cannot reset it) `allAgentsIdleSinceAt` + `lastGoalNudgeAt`. On any effective-live busy agent clear `allAgentsIdleSinceAt`; on the one busy→all-idle edge set it once. Goal eligibility requires `now - allAgentsIdleSinceAt >= GOAL_NUDGE_IDLE_INTERVAL_MS` (default 60s, env), then repeats only at full intervals from `lastGoalNudgeAt` (max-3/backoff stays but counts interval emissions, never ticks). If graph path A is eligible, emit ONLY graph nudge and defer/suppress goal (`suppressed_by_actionable_graph`). Before surfacing a queued nudge, re-read live state; suppress it if its predicate became false (node assigned / agent busy) — no stale “no active work” body. **Files.** `extensions/swarm/src/reconcile.ts`, `extensions/swarm/src/types.ts`, `extensions/swarm/src/constants.ts`, `extensions/swarm/idle-nudge.test.mjs`, `extensions/swarm/task-liveness.test.mjs`. **Acceptance.** (1) unfinished actionable graph + all idle → exactly graph nudge, zero goal nudge; (2) 30s idle with 60s interval and many pump ticks → zero goal nudges; (3) 60s continuous idle/no graph work → one goal nudge; (4) 5s later → no second goal nudge; (5) busy at t=59 resets epoch, requiring fresh full 60s; (6) stale/dead ghosts do not reset epoch; (7) queued nudge whose node is assigned before surface is suppressed. **Impact/effort.** CRITICAL (user-specified core nudge semantics) / M. | Pending |

## Cross-issue invariants

These rules apply throughout the queue:

```text
- task.json remains task-graph source of truth
- mailbox delivery is at-least-once and must be idempotent
- transport delivery, response protocol, and node execution are separate state machines
- readiness derivation remains pure; graph reopening is an explicit, audited transition
- only declared rework:true edges may re-enter terminal nodes
- a failed ordinary dependency never means successful dependency satisfaction
- stale is advisory; harness never infers semantic done/failed from process/pane idleness
- retry, reminder, reassign, cancellation, and supersession remain distinct actions
- no automatic reassign or semantic completion without an explicit task policy and authority decision
- every recovery nudge is semantically deduplicated, bounded, and action-oriented
```

## UAT protocol

For each issue create an isolated validation target, e.g.:

```bash
tmux new-session -d -s swarm-uat-issue-<n> -n pi
```

Start fresh Pi with the **project-configured default model/provider**; do not hard-code a different pool unless the UAT itself is specifically testing provider fallback:

```bash
PI_SWARM_IS_ORCHESTRATOR=1 pi -e extensions/swarm/index.ts
```

Capture before/after panes and record:

```text
tmux target
exact Pi command
scenario actions
expected vs observed result
snapshot/log paths
any failure and corrective follow-up
```

The UAT must include at least one negative assertion for the issue (for example: worker `force=true` is rejected; stale assignment result cannot mutate a newer lease; cancelled assignment cannot close a node).

## Decision log

- Execute the roadmap one issue at a time, not through a single broad implementation task.
- Use the configured default model/provider for assigned agents.
- Do not delete historical `.pi/swarm/` runtime state.
- Do not expand the model-facing tool surface merely to support recovery operations; prefer validated orchestrator actions/commands when possible.
