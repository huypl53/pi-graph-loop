// === swarm/nudges/ack.ts — root nudge acknowledgment helpers (Phase 6) ===
// ackRootNudgeLocked and ackRootGraphAdvanceNudgesLocked are internal functions in
// graph-advance.ts (not exported). They are called internally within reconcile/evaluate
// functions and do not need to be re-exported. This stub documents their location.
//
// If these need to be exposed for testing or extension, export them from graph-advance.ts first.

export {};
