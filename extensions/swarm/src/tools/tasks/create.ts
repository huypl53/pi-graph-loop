// === swarm/tools/tasks/create.ts — swarm_create_task submodule ===
// The swarm_create_task tool + qualification gate logic lives in ../tasks.ts.
// This module exists for organizational clarity; the canonical implementation is there.
// Consumers: import registerTasksTools from ../tasks.ts or ../index.ts.

export { registerTasksTools } from "../tasks.ts";
