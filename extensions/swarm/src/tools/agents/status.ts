// === swarm/tools/agents/status.ts — swarm_agent_status + swarm_list_agents submodule ===
// The status tools are registered inside registerAgentsTools in ../agents.ts.
// This module exists for organizational clarity; the canonical implementation is there.

export { registerAgentsTools } from "../agents.ts";
