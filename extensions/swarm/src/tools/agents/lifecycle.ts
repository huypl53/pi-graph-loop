// === swarm/tools/agents/lifecycle.ts — swarm_spawn_agent + swarm_stop_agent submodule ===
// The lifecycle tools are registered inside registerAgentsTools in ../agents.ts.
// This module exists for organizational clarity; the canonical implementation is there.

export { registerAgentsTools } from "../agents.ts";
