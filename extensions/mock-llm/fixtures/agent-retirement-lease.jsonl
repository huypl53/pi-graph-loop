# agent-retirement-lease: Issue 82 — task-close sweep honors a `leaseKind: "park"` lease.
# The sweep must PAUSE (not stop) the worker so the pane is preserved for inspection.
{"name":"orchestrator closes task with parked lease","events":[{"type":"text","delayMs":15,"text":"Closing task; the worker is parked (not stopped) because the lease is valid.","chunks":["Closing task; ","the worker is parked (not stopped) ","because the lease is valid."]},{"type":"toolcall","delayMs":15,"name":"swarm_update_task","id":"close-1","arguments":{"taskId":"task-issue82-lease","nodeId":"only-node","status":"done","outcome":"implemented","note":"Closed with park lease active; sweep should pause worker-b."}},{"type":"stop","delayMs":5,"reason":"stop"}]}
