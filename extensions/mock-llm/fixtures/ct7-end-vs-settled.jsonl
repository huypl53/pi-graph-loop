# ct7-end-vs-settled: CT-7 probe — queue follow-up + emit agent_end;
# agent_settled fires LATER (after the follow-up), not before.
#
# Reproduces the F7 footgun (pi-runtime-evidence.md:11-30) at the Pi runtime
# contract boundary. The fixture is documentary: the real assertion logic
# lives in `extensions/swarm/ct-phase2-probes.test.mjs` §CT-7, which mocks
# the lifecycle emitter directly and counts R10-1 boundary calls. This
# fixture exists so a future real-lane validation (`pi --provider mock-llm
# --model ct7-end-vs-settled`) reproduces the same scripted turn shape
# end-to-end.
#
# Deterministic: scripted turns + delayMs gates. The probe outcome table is
# printed by the test driver after the lane completes.
{"name":"ct7 baseline (register lifecycle observer + queue followUp injection)","stopReason":"toolUse","events":[{"type":"text","delayMs":15,"text":"CT-7 baseline: the probe lane subscribes to agent_end and agent_settled lifecycle hooks, captures the emission order into a timeline array, and queues a followUp deliverAs via pi.sendMessage to ensure a continuation fires later. The test driver mocks this boundary: pi.sendMessage is stubbed to record the followUp injection; the fake lifecycle emitter is wired to the pi.on subscriptions; the timeline array accumulates agent_end / followUp_consumed / agent_settled entries in tick order.","chunks":["CT-7 baseline: ","the probe lane subscribes to agent_end and agent_settled lifecycle hooks, ","captures the emission order into a timeline array, ","and queues a followUp deliverAs via pi.sendMessage ","to ensure a continuation fires later."," The test driver mocks this boundary: ","pi.sendMessage is stubbed to record the followUp injection; ","the fake lifecycle emitter is wired to the pi.on subscriptions; ","the timeline array accumulates agent_end / followUp_consumed / agent_settled entries ","in tick order."]}]}
{"name":"ct7 mid-stream agent_end assertion (agent_end observed; agent_settled NOT yet)","stopReason":"toolUse","events":[{"type":"text","delayMs":25,"text":"CT-7 mid-stream assertion: while the current assistant turn is ending, the fake emitter fires agent_end on the current tick. The test driver asserts: agentEndEmittedCount >= 1; agentSettledEmittedCount === 0; timeline ends with [agent_end] (no agent_settled yet).","chunks":["CT-7 mid-stream assertion: ","while the current assistant turn is ending, ","the fake emitter fires agent_end on the current tick."," The test driver asserts: ","agentEndEmittedCount >= 1; ","agentSettledEmittedCount === 0; ","timeline ends with [agent_end] ","(no agent_settled yet)."]}]}
{"name":"ct7 follow-up consumed + agent_settled (agent_settled AFTER follow-up)","stopReason":"toolUse","events":[{"type":"text","delayMs":30,"text":"CT-7 post-followUp assertion: after the queued follow-up is consumed, the fake emitter fires agent_settled on the next tick. The test driver asserts: agentSettledEmittedCount >= 1; emission ordering is [agent_end, followUp_consumed, agent_settled]; agentSettledTimestamp > followUpConsumedTimestamp. Boundary counters: agentEndEmittedCount >= 1; agentSettledEmittedCount >= 1; agentSettledAfterFollowUp === true.","chunks":["CT-7 post-followUp assertion: ","after the queued follow-up is consumed, ","the fake emitter fires agent_settled on the next tick."," The test driver asserts: ","agentSettledEmittedCount >= 1; ","emission ordering is [agent_end, followUp_consumed, agent_settled]; ","agentSettledTimestamp > followUpConsumedTimestamp."," Boundary counters: ","agentEndEmittedCount >= 1; ","agentSettledEmittedCount >= 1; ","agentSettledAfterFollowUp === true."]}]}
{"name":"ct7 settle","stopReason":"stop","events":[{"type":"text","delayMs":10,"text":"CT-7 probe complete. The probe driver records the outcome table (CONTRACT_CONFIRMED / BUG_FOUND_R_ROW_NEEDED) per sub-case based on the R10-1 boundary counters at the mock pi boundary.","chunks":["CT-7 probe complete."," The probe driver records the outcome table ","(CONTRACT_CONFIRMED / BUG_FOUND_R_ROW_NEEDED) per sub-case ","based on the R10-1 boundary counters."]},{"type":"stop","delayMs":5,"reason":"stop"}]}
