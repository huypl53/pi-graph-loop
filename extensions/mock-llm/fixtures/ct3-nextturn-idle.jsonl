# ct3-nextturn-idle: CT-3 probe — idle agent + sendMessage({nextTurn, triggerTurn:true})
# does NOT start a turn; message only surfaces on next user prompt.
#
# Reproduces the F8 footgun (pi-runtime-evidence.md:11-30) at the Pi runtime
# contract boundary. The fixture is documentary: the real assertion logic
# lives in `extensions/swarm/ct-phase2-probes.test.mjs` §CT-3, which mocks
# the pi boundary directly and counts R10-1 boundary calls. This fixture
# exists so a future real-lane validation (`pi --provider mock-llm --model
# ct3-nextturn-idle`) reproduces the same scripted turn shape end-to-end.
#
# Deterministic: scripted turns + delayMs gates. The probe outcome table is
# printed by the test driver after the lane completes.
{"name":"ct3 baseline (capture pi+ctx; synchronously send nextTurn injection)","stopReason":"toolUse","events":[{"type":"text","delayMs":15,"text":"CT-3 baseline: in a real lane, the probe extension would capture pi+ctx into module-level vars and synchronously call capturedPi.sendMessage({ customType: ct3-nextturn, content: queued, display: true, details: {} }, { deliverAs: nextTurn, triggerTurn: true }). Per CT-1 contract the call returns void; the wrapper is invoked on the same tick. The test driver mocks this boundary and asserts sendMessageCallCount === 1, sendMessageReturnIsUndefined === true, deliverAs === nextTurn, triggerTurn === true.","chunks":["CT-3 baseline: ","in a real lane the probe extension captures pi+ctx ","and synchronously calls capturedPi.sendMessage({ customType: ct3-nextturn, ... }, ","{ deliverAs: nextTurn, triggerTurn: true })."," Per CT-1 contract ","the call returns void; ","the wrapper is invoked on the same tick."," The test driver mocks this boundary ","and asserts sendMessageCallCount === 1, ","sendMessageReturnIsUndefined === true, ","deliverAs === nextTurn, ","triggerTurn === true."]}]}
{"name":"ct3 idle assertion (ctx.isIdle() === true; sendMessageCallCount frozen)","stopReason":"toolUse","events":[{"type":"text","delayMs":25,"text":"CT-3 idle assertion: after the nextTurn injection, ctx.isIdle() must remain true for 250ms (no turn was implicitly triggered). The test driver asserts isIdleAfterNextTurnSend === true; turnCursorAdvancedSinceInjection === false. Boundary counters: sendMessageCallCount stays at 1 (no new sendMessage from the queued injection); nextTurnTriggerTurnWasIgnored === true.","chunks":["CT-3 idle assertion: ","after the nextTurn injection, ","ctx.isIdle() must remain true for 250ms ","(no turn was implicitly triggered)."," The test driver asserts isIdleAfterNextTurnSend === true; ","turnCursorAdvancedSinceInjection === false."," Boundary counters: ","sendMessageCallCount stays at 1 ","(no new sendMessage from the queued injection); ","nextTurnTriggerTurnWasIgnored === true."]}]}
{"name":"ct3 next-user-prompt surfaces (queued nextTurn message present in L4 context)","stopReason":"toolUse","events":[{"type":"text","delayMs":20,"text":"CT-3 surface assertion: when a real user prompt arrives later, the previously-queued nextTurn message surfaces in the L4 context. The test driver simulates the next user turn by adding a user-role message + the queued custom message to the request context, then asserts the custom message with customType === ct3-nextturn is present in request.messages. Boundary counters: surfacesOnNextUserPrompt === true; contextIncludesQueuedCustom === true.","chunks":["CT-3 surface assertion: ","when a real user prompt arrives later, ","the previously-queued nextTurn message surfaces in the L4 context."," The test driver simulates the next user turn ","by adding a user-role message + the queued custom message to the request context, ","then asserts the custom message with customType === ct3-nextturn is present in request.messages."," Boundary counters: ","surfacesOnNextUserPrompt === true; ","contextIncludesQueuedCustom === true."]}]}
{"name":"ct3 settle","stopReason":"stop","events":[{"type":"text","delayMs":10,"text":"CT-3 probe complete. The probe driver records the outcome table (CONTRACT_CONFIRMED / BUG_FOUND_R_ROW_NEEDED) per sub-case based on the R10-1 boundary counters at the mock pi boundary.","chunks":["CT-3 probe complete."," The probe driver records the outcome table ","(CONTRACT_CONFIRMED / BUG_FOUND_R_ROW_NEEDED) per sub-case ","based on the R10-1 boundary counters."]},{"type":"stop","delayMs":5,"reason":"stop"}]}
